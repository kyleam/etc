if [ $(whoami) != "root" ]; then
    export WORKON_HOME=$HOME/.pyenv
    ## debian
    source /etc/bash_completion.d/virtualenvwrapper
fi

alias ipy='ipython3'

alias pt2='py.test'
alias pt='py.test-3'

alias ns2='nosetests'
alias ns='nosetests3'
